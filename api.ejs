import { requestClient } from '#/api/request';

<% apikeyArr.forEach(function(key, index){ %>
<%- `export async function get${key.split('-')[1].charAt(0).toUpperCase() + key.split('-')[1].slice(1)}() {` %>
  return requestClient.get('/mock/<%= key.split('-').join('') %>');
<%- `}` %>
<% }); %>