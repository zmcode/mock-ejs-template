import { requestClient } from '#/api/request';

<% apikeyArr.forEach(function(key){ %>
<%- `export async function get${key.charAt(0).toUpperCase() + key.slice(1)}() {` %>
  return requestClient.get('/mock/<%= key %>');
<%- `}` %>
<% }); %>